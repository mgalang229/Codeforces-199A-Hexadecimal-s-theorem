#include <bits/stdc++.h>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	int n;
	cin >> n;
	// you can output 0, 0, n because the number given is always a Fibonacci number
	cout << "0 0 " << n << '\n';
	return 0;
}
